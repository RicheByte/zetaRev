version: '3.8'
services:
  dashboard:
    build: ./dashboard
    ports:
      - "5000:5000"
    volumes:
      - ./reports:/opt/reports:ro
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
